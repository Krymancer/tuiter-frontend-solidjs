import { Component } from "solid-js";

const Logo : Component = () => {
        return <div class="flex cursor-pointer">
            <a class="flex flex-row font-bold text-4xl">Tui<pre class="text-brand">ter</pre></a>
        </div>
};

export default Logo;
